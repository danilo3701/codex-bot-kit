# CONTEXT PACK (Template)

## 0) Goal
Generate a project-specific `/AGENTS.md` from `FOR_AGENTS_TEMPLATE.md`.
Do not invent facts. If missing info — ask max 3 questions.

## 1) Project snapshot
- Project name:
- One-line purpose:
- Target platform (Telegram bot / web / API / etc):
- Users (who uses it):
- Main flows (3 bullets):

## 2) Repo structure (paste tree or list)
- Modules root path:
- Docs root path:
- Entry points (files that start the app):
- Where configs live:
- Where database/models live (if any):

## 3) Tech constraints (if known)
- Language:
- Framework (aiogram/pyTelegramBotAPI/etc):
- Storage (SQLite/Postgres/Redis/etc):
- Hosting (optional):

## 4) Work rules (critical)
- I do NOT read code. Verification must be: self-check + manual checklist steps.
- Must follow workflow: PLAN → BUILD → REVIEW/VERIFY.
- Must enforce ALLOWLIST per task.
- tests.md is append-only.

## 5) Definitions (optional)
- Module naming pattern:
- Docs naming pattern:

## 6) Current task (optional)
- What feature we are about to build:
- What module it belongs to:
